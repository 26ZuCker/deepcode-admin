import {
  get_course_all,
  get_schedule_all,
  get_clock_all,
  course_data,
  clock_data,
  schedule_data,
} from '@/apis/schedule/schedule.js';

export const state = () => ({});

export const mutations = {};

export const actions = {};

export const getters = {};
